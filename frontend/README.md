# Frontend Directory

This directory is part of the main repository. It was previously configured as a git submodule but has been converted to a regular directory to resolve CI build issues.

The frontend application files will be placed here as the project develops.
